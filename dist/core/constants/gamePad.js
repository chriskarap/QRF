System.register([], function (_export, _context) {
  "use strict";

  return {
    setters: [],
    execute: function () {
      const VR = 'vr';

      _export('VR', VR);

      const NON_VR = 'nonvr';

      _export('NON_VR', NON_VR);

      const BOTH = 'both';

      _export('BOTH', BOTH);
    }
  };
});