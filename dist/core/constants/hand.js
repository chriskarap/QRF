System.register([], function (_export, _context) {
  "use strict";

  return {
    setters: [],
    execute: function () {
      const LEFT = 'left';

      _export('LEFT', LEFT);

      const RIGHT = 'right';

      _export('RIGHT', RIGHT);
    }
  };
});