System.register([], function (_export, _context) {
  "use strict";

  return {
    setters: [],
    execute: function () {
      /**
       * RODIN STARTED "rodinstarted"
       * @type {string}
       */
      const RODIN_STARTED = 'rodinstarted';

      /**
       * REQUEST RODIN STARTED "requestrodinstarted"
       * @type {string}
       */

      _export('RODIN_STARTED', RODIN_STARTED);

      const REQUEST_RODIN_STARTED = 'requestrodinstarted';

      /**
       * RENDER START "renderstart"
       * @type {string}
       */

      _export('REQUEST_RODIN_STARTED', REQUEST_RODIN_STARTED);

      const RENDER_START = 'renderstart';

      /**
       * RENDER "render"
       * @type {string}
       */

      _export('RENDER_START', RENDER_START);

      const RENDER = 'render';

      /**
       * RENDER END "renderend"
       * @type {string}
       */

      _export('RENDER', RENDER);

      const RENDER_END = 'renderend';

      /**
       * ACTIVE SCENE "activescene"
       * @type {string}
       */

      _export('RENDER_END', RENDER_END);

      const ACTIVE_SCENE = 'activescene';

      /**
       * REQUEST ACTIVE SCENE "requestactivescene"
       * @type {string}
       */

      _export('ACTIVE_SCENE', ACTIVE_SCENE);

      const REQUEST_ACTIVE_SCENE = 'requestactivescene';

      _export('REQUEST_ACTIVE_SCENE', REQUEST_ACTIVE_SCENE);

      const REQUEST_HMD_POSE = 'requesthmdpose';

      _export('REQUEST_HMD_POSE', REQUEST_HMD_POSE);

      const HMD_POSE = 'hmdpose';

      _export('HMD_POSE', HMD_POSE);

      const POST_MESSAGE_TRANSPORT_PARENT_ID = 'postmessagetransportparentid';

      _export('POST_MESSAGE_TRANSPORT_PARENT_ID', POST_MESSAGE_TRANSPORT_PARENT_ID);

      const ALL_SCULPTS_READY = 'allsculptsready';

      _export('ALL_SCULPTS_READY', ALL_SCULPTS_READY);
    }
  };
});